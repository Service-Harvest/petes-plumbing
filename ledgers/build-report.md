# Build Report — Pete's Plumbing

> Generated in Phase 2, updated by Phases 5, 7, 8, and read in full by
> Phase 13 for the final report to the user.

## Connection Status
- **DataforSEO**: Initially NOT CONNECTED — `mcp__dataforseo__business_data_business_listings_search` and `mcp__dataforseo__serp_locations` both returned HTTP 401 during the original Phase 1/Phase 2 checks (traced to missing `DATAFORSEO_USERNAME`/`DATAFORSEO_PASSWORD` env vars feeding the MCP server's Basic Auth header). User fixed the config; reconnection confirmed working as of 2026-07-16 — both endpoints now return real data. Phase 1/Phase 2 findings recorded before the fix are labeled "estimated — not DataforSEO-verified" and should be re-verified with live DataforSEO data before final sign-off, since the original research ran on the web-search fallback.
- **Gemini**: Initially NOT CONNECTED — `GEMINI_API_KEY` was unset during Phase 4a (favicon generation), confirmed via a direct test call returning HTTP 403 `PERMISSION_DENIED` ("unregistered caller," i.e. no key presented at all — not an invalid-key or quota error). The Phase 4a favicon used a hand-authored SVG mark (`/site/assets/img/favicon.svg`) as a placeholder fallback as a result. User set `GEMINI_API_KEY` before Phase 7 started; reconnection confirmed working as of 2026-07-16 via a direct test call to `gemini-2.5-flash-image` returning HTTP 200 with real image data. Phase 7 proceeds with real Gemini-generated images. The Phase 4a favicon placeholder remains in place (not retroactively regenerated) since Phase 4a has already completed — noting this in the Phase 13 final report as an optional follow-up if the client wants a Gemini-generated favicon instead of the hand-authored SVG mark.

## Research Takeaways
(Notable findings from Phase 2 and Phase 5 research, per category/page)
- Phase 2 (original pass): All signals estimated via general web search due to DataforSEO 401s at the time.
- Phase 2 (re-run with live DataForSEO, 2026-07-16): Real search volume, SERP, and AI/LLM signals replaced the estimates above. Key findings:
  - **Water Heater Installation**: 27,100/mo (US), low competition, keyword difficulty 8 — confirms this manual override is a strong pick.
  - **Drain Cleaning** ("drain cleaning near me"): 74,000/mo (US), very low competition (0.1) — strongly confirms Drainage Service priority.
  - **Gas Line Repair**: 2,900/mo (US), medium competition, kd 13. Real Armonk-area local-pack results are dominated by general Westchester plumbers (All Pipes Repair, VP's Plumbing, ABS Plumbing), not gas-only specialists — confirms Gasfitter is winnable territory for a full-service plumber. Note: "Gasfitter" itself is a weak/anomalous search term (mixed-language artifact in the data) — the category page should lead with "Gas Line Repair" and "Gas Water Heater Installation" language, not the word "Gasfitter," since that's not how customers actually search.
  - **Emergency Plumber / Emergency Plumber Near Me**: 60,500–90,500/mo (US), very low competition, transactional intent. Real Armonk-local SERP is dominated by general plumbers/drain companies (Reliable Drain Solutions, All Pipes Repair, Armonk Plumbing Services Inc, Roto-Rooter, Mr. Rooter, Bleakley) — winnable, not a specialist-dominated niche. **New recommendation: added Emergency Plumbing Repair as a 5th priority pick** (upgraded from Tier 3 to Tier 1 in architecture.md) — this was underweighted in the original web-search-only pass.
  - **Water Damage Restoration**: real national volume is substantial (135,000/mo) with a low PPC-competition score, but the real Armonk-local organic + local-pack SERP is 100% dominated by certified restoration specialists (Green Restoration, Green Island Group [IICRC-certified], ServiceMaster, UWRG Westchester) — zero general plumbers visible. Confirms the original recommendation: do not build this as a standalone page: high volume alone doesn't override a competitive field this client's intake can't credibly compete in.
  - **Bathroom Remodeling**: 135,000/mo (US), medium competition, kd 38. Real Armonk-local SERP dominated by dedicated remodeling firms (WLD Bathroom Remodeling, Bath Replacers, Raj Kitchen and Bath) — confirms standard (non-priority) page treatment.
  - **AI/LLM visibility** (ChatGPT/gpt-4o, web search on): asked "Who's the best emergency plumber in Armonk, NY?" — response cited 6 competitors, all sourced from Angi's Armonk plumbing directory listing (Mid-Westchester Sewer & Drain, Winners Sewer & Drain, Atomic Rooter, Aldos Plumbing, In Town Plumbing and Heating, NYC Plumbing). None is Pete's Plumbing (expected — pre-launch). Takeaway: getting/maintaining a strong Angi listing matters for AI-answer visibility on emergency-plumbing queries, independent of the site itself.
- Bathroom & Kitchen Remodeling and Septic Services remain real GBP categories with real demand, but both are dominated by specialists offering a broader scope than this client's intake supports — kept as standard pages, not homepage-linked priority.

## Content Quality Notes
(Notable QA findings from Phase 8, per page)
- Homepage, Contact: clean pass, no changes.
- Services Hub: **factual-accuracy fix** — removed an invented company-history claim ("this is where the company started") not supported by intake.
- About: reading-level fix — split two overly long, clause-stacked sentences in the local-experience section.
- Bathroom & Kitchen Remodeling (category): clean pass; scope-honesty disclaimer already solid.
- Bathroom Plumbing Remodeling, Kitchen Plumbing Remodeling: added an explicit "plumbing work only, not tile/cabinetry/design" sentence to each — the category page had this but the child pages only implied it.
- Water Heater Installation, Emergency Plumbing Repair (Tier 1): reading-level fixes — split overloaded/compound sentences given these pages' outsized traffic share.
- Hot Water System Repair, Water Line Replacement, Water Main Repair, Fixture Replacement, Water Softener Installation, Water Filtration System Installation, Slab Leak Repair, Frozen Pipe Repair, Low Water Pressure Repair, Sink Installation: clean pass, no changes.
- Sump Pump Installation: **claims-compliance fix** — removed an unsourced specific pump-lifespan figure ("7-10 years") not supported by intake.
- Bathtub Installation: grammar fix ("a acrylic" → "an acrylic" in an FAQ heading).
- Commercial Plumbing Services, Repiping, Plumbing Inspection, Appliance Hook-Up, Urinal Installation, Rainwater Tank Installation, Water Leak Sensor Installation, Plumbing Maintenance, Backflow Testing: clean pass, no changes.
- Boiler Installation: grammar fix (duplicated word "a a seasonal...").
- Laundry Room Plumbing: grammar fix (dangling modifier in hero subheadline).
- **Cross-cutting observation (not fixed, flagged for awareness)**: several installation-type "process" section openers across the Plumber-batch-B pages use a similar "We start by X. From there, we Y..." construction. Each page passes individually, but noted here in case a future copy refresh wants more variety.
- Drainage Service (category): reading-level fix — simplified a dense sentence about plumbing codes.
- Drain Cleaning, Clogged Drain Repair, Drain Snaking/Rooter Service, Hydro Jetting, Grease Trap Cleaning: clean pass, no changes.
- Sewer Line Repair: reading-level fix — simplified a long InterNACHI-sourced sentence.
- Sewer & Drain Camera Inspection: **fixed a missing required internal link** — the anchor ledger specified a body-copy link to `/drainage-service-armonk-ny` ("additional drain and sewer services") that was only present in breadcrumb/footer, not actual body copy. Added it.
- Gasfitter (category): fixed a broken outbound-citation sentence (treated "codes" as an organization name; now correctly names IAPMO). Confirmed no scope drift — still leads with "gas line repair"/"gas water heater" language over "Gasfitter."
- Gas Line Repair, Gas Water Heater Installation: clean pass, no changes.
- Septic Services (category): fixed a mangled internal-anchor sentence and a duplicated "EPA's EPA" outbound citation. Confirmed no literal duplication of local framing vs. child page.
- Septic Tank Service: fixed a near-verbatim duplicate FAQ answer shared with the category page (same disclosure, reworded for variation). Scope-honesty (plumbing-side-only, not a pumping company) confirmed intact on both septic pages.
- Plumber batch A (Tankless Water Heater Installation through Shower Valve Replacement): QA surfaced a systemic Phase 6 drafting issue — 5 of 12 pages had a fabricated or wrong-domain outbound citation URL (confirmed via live HTTP check, not just domain-name plausibility). Fixed: `leak-detection` (fake NACHI deep link), `pipe-repair` (wrong PHCC domain — archived local chapter vs. real phccweb.org), `burst-pipe-repair` (fabricated date-coded FEMA press-release URL), `outdoor-faucet-repair` (fake This Old House deep link), `shower-installation` (fake NAHB deep link). `garbage-disposal-repair` and `shower-valve-replacement` outbound links were unverifiable (403/bot-blocked even at domain root) and precautionarily downgraded to safe root-domain links.
- **Sitewide outbound-citation sweep** (triggered by the above finding — checked every one of the 39 unique outbound URLs across all 57 pages via live HTTP request, not just domain plausibility): found 7 MORE confirmed-fabricated/broken deep links beyond what the batch-A QA agent already caught, all fixed by replacing with verified-working root-domain URLs:
  - `water-softener-installation`: fake Consumer Reports water-softeners path → consumerreports.org/appliances/
  - `boiler-installation`: fake energy.gov furnaces-and-boilers path → energy.gov root
  - `hot-water-system-repair`: fake energy.gov water-heating path → energy.gov root
  - `grease-trap-cleaning`: fake EPA food-service-facilities path → epa.gov root
  - `urinal-installation`: fake EPA WaterSense toilets/urinals path → epa.gov/watersense (real page)
  - `plumbing-maintenance`: fake This Old House checklist path → thisoldhouse.com/plumbing (real page)
  - `water-leak-sensor-installation`: fake UL "ul-mark" resource path → ul.com root
  - After these fixes, re-verified all 39 outbound URLs: zero 404s remain. Remaining non-200 responses (angi.com, cdc.gov, familyhandyman.com, fema.gov, hgtv.com) are confirmed bot-blocking (403 even at bare domain root, not path-specific), not fabricated links — left as-is since they're legitimate approved domains.
  - Separately, confirmed via live page-content comparison that `phcc.org` (used by 3 pages) actually resolves to an unrelated Northern Virginia local chapter site, not the National Association — the real national org is `phccweb.org`. Updated all 4 PHCC citations sitewide to `phccweb.org` and corrected the approved domain list in `phase-06-content-drafting.md` to prevent this recurring on future edits.

## Phase 9 (Schema) + validator hardening
- Generated JSON-LD schema for all 57 pages via a script (not hand-authored per page, to guarantee sitewide `@id` consistency): LocalBusiness/WebSite on homepage, WebPage/CollectionPage/AboutPage/ContactPage + BreadcrumbList on every page, Service schema on all 53 category/service pages, ItemList on the hub, FAQPage on all 54 pages with visible FAQs. Verified: 0 invalid JSON, 0 prohibited schema types (Review/Product/Article), breadcrumb chains match the approved architecture and each page's actual visible breadcrumb.
- Caught and fixed a real bug in my own schema-generation script during this process: the meta-description regex stopped at the first apostrophe (mistaking a contraction like "home's" for a closing quote), truncating descriptions mid-word. Fixed and regenerated cleanly.
- **Found and fixed 3 real bugs in `scripts/validate.js` itself** (the reusable, cross-client validation gate) while running it as an early sanity check:
  1. The ledger-internal duplicate-anchor check used a raw substring search instead of comparing exact table-cell values, causing false-positive failures whenever one legitimate anchor text happened to contain a different, shorter legitimate anchor as a substring (e.g. "everything else we handle" vs. "everything else we handle for Armonk plumbing"). Fixed to compare exact parsed values.
  2. The markdown-table parser didn't strip the backticks used to format file paths as inline code (e.g. `` `/some-page` ``), so essentially every ledger-row lookup against real page content was silently failing — meaning the "every required link actually exists in body copy" check had never actually verified anything all build. Fixed, then found (and fixed) 2 real missing/mismatched required links this surfaced: `tankless-water-heater-installation` and `plumbing-maintenance` had the right links but slightly different wording (an added word, a capitalization difference) than the ledger recorded — reconciled the ledger to match the actual (correct, natural-reading) page text in both cases.
  3. A trailing-slash mismatch between how page URLs were stored (`/page-slug/`) vs. looked up (`/page-slug`, normalized) meant the same required-link check silently skipped every non-homepage page. Fixed by normalizing consistently at both write and read.
  4. Added a legitimate exemption: CTA buttons (`class="btn"`) intentionally repeat the same label sitewide ("Request a Free Estimate") as a UI/UX convention, not as SEO anchor text — updated the validator to treat them like nav/footer (structural, exempt from the anchor-uniqueness rule) rather than failing the whole site on this by design.
- **Fixed a real, sitewide structural bug found via browser inspection**: the header's site-nav `<ul>` never got its CSS applied (`.site-nav ul` selector targeted a nonexistent wrapper — the class is on the `<ul>` itself), so the nav rendered as a bulleted list on all 57 pages. Also moved the header's logo link inside the `<nav>` element (was a `<div>`) so it's correctly treated as navigation rather than a body-copy anchor repeating "Pete's Plumbing" as anchor text on every page.
- Fixed 3 real orphan pages (no incoming body-copy links): added one natural contextual link into each of Boiler Installation, Laundry Room Plumbing, and Rainwater Tank Installation from a genuinely related page.

## Phase 10 (GHL Embeds)
- **Chat widget**: inserted sitewide (all 57 pages + 404.html), placed just before `</body>` so it doesn't block rendering of page content. Verified in-browser: renders as a floating chat bubble, no layout shift to surrounding content.
- **Free estimate form**: initially placed only on the Contact page (auto-show popup), per the scope reasoning below. **Superseded** — see "Post-launch fix" below: the client requested (and I implemented) a click-triggered popup wired to every "Request a Free Estimate" CTA sitewide instead.
  - Original reasoning (kept for history): the client's embed code was configured `data-trigger-type="alwaysShow"` / `data-activation-type="alwaysActivated"`, which (confirmed via live browser test) made it auto-open as a full-page modal popup immediately on load. Placing that on all 53 category/service pages would have meant every page auto-popping a modal — a bigger UX change than the existing CTA design (a plain link to `/contact`) called for, so it was scoped to the Contact page only at the time.

## Post-launch fix (requested after initial deploy)
- Updated `pipeline/phase-10-ghl-embeds.md`: the free-estimate embed should be wired to trigger on every "Request a Free Estimate" (or equivalent) CTA sitewide — top/mid/final placements, every page — not just placed on the Contact page. Chat widget placement (sitewide) is unchanged.
- Applied to the live build: changed the popup's config from `data-trigger-type="alwaysShow"` to `data-trigger-type="click"` with `data-trigger-value=".request-estimate-cta"`. Added the `request-estimate-cta` class to all 110 existing "Request a Free Estimate" links/buttons across all 57 pages (kept each one's existing `href` — mostly `/contact`, or `tel:` on the Contact page itself — as a graceful-degradation fallback if the popup script fails to load). Moved the popup widget itself from Contact-page-only to sitewide (once per page, alongside the chat widget), so every page has something for the click to open. Removed the old Contact-page inline always-show iframe (now redundant/duplicate) and replaced that section with a plain CTA button using the same shared trigger class.
- Verified in-browser: clicking "Request a Free Estimate" on a regular service page (not just Contact) now opens the popup directly, with no auto-open on page load anywhere.
- Re-ran `scripts/validate.js`: clean, 0 failures. Pushed through the normal deploy workflow; both `validate` and `deploy` jobs succeeded.

## Post-launch fix #2: root-relative paths broke under the GitHub Pages fallback subpath URL
**Root cause**: the entire site was built with root-absolute internal paths (`href="/services"`, `href="/assets/css/main.css"`, `src="/assets/img/..."`), which is correct for a real custom domain serving from its own root. But `hexorasystems.com` (the intake's test domain) was never actually verified with GitHub Pages, so the live site was being served from GitHub's fallback project-subpath URL (`https://service-harvest.github.io/petes-plumbing/`) instead. Root-absolute paths resolve against the *domain* root under that fallback, not the *project* root where the files live — so every nav link, footer link, in-body link, the stylesheet, the favicon, and every image 404'd sitewide. This was not a Phase 4a/Phase 7 authoring defect (the CSS was fully written, the images all existed at the right paths) — it was a structural assumption that only breaks under this specific fallback hosting mode.

**Fix applied**: converted every internal `href`/`src` sitewide to depth-aware relative paths (`./` on the homepage/404.html, `../` on every other page, since all 53 category/service/hub/about/contact pages live exactly one directory below site root) — these resolve correctly under *either* a real custom domain at root *or* a GitHub Pages project subpath, so this isn't just a one-off patch. Canonical tags, OG/Twitter meta tags, and JSON-LD schema URLs were deliberately left fully absolute (`https://hexorasystems.com/...`), since those should always declare the page's real identity regardless of which URL it's being viewed through.

**Validator fix required too**: `scripts/validate.js` itself wasn't fully relative-path-aware — one check (orphan-page tracking) already stripped a leading `./`/`../` before comparing, but two others (the broken-link check and the anchor-ledger required-link check) didn't, so converting the site's paths caused ~1,400 false-positive failures in the validator until fixed. Replaced the ad-hoc regex stripping with a proper `resolveHref()` helper (using real URL-resolution semantics via Node's `URL` class) and applied it consistently everywhere an href is compared against a page URL.

**Template fix**: updated `pipeline/phase-04-repo-scaffold.md` (new Task 2, Layer 1 requirement), `pipeline/phase-06-content-drafting.md`, and all three `/snippets/` reference files to document and use this relative-path (`{{ROOT}}`) convention as the standard going forward, so future client builds don't hit the same bug when previewed via the GitHub Pages fallback URL before DNS is cut over.

**Verified**: re-ran `scripts/validate.js` (clean, 0 failures) and built a local test server that mimics the exact GitHub Pages subpath structure (`/petes-plumbing/` prefix) — confirmed in-browser that the homepage renders fully styled, the hero image loads, the chat widget loads, and clicking through to another page (Services) works correctly.
- **Google review widget, GBP embed**: left as placeholders on the homepage, per explicit intake instruction ("leave as placeholders").

## Phase 11 (Sitemap + Technical Pass)
- Generated `sitemap.xml` with all 57 pages, `<lastmod>2026-07-17</lastmod>` (initial launch date).
- Generated a persistent 64-char hex IndexNow key at `site/indexnow-key.txt` — reuse this exact key on any future Phase 14 edit, never regenerate it.
- Expanded `llms.txt` with the complete 53-page listing grouped by category (was previously just the 4 core nav pages, per Phase 4's note that this would happen once all pages existed).
- Confirmed robots.txt/CNAME/sitemap domain all consistently reference `hexorasystems.com`.
- Re-ran `scripts/validate.js` with the sitemap now present: all sitemap-cross-check rules pass (every sitemap URL is a real page with a matching canonical, every real page is listed in the sitemap).

## Phase 12 (Validation Gate)
Final gate run: **0 hard failures, 86 non-blocking warnings** (all title/meta-description length guidance — every title/meta was pre-approved in the Phase 3 architecture checkpoint, so these are accepted as-is rather than rewritten purely to hit a character-count target). Clean to proceed to deploy.

## Phase 13 (Deploy)
- Created `Service-Harvest/petes-plumbing` on GitHub (public), pushed, set Pages source to GitHub Actions build.
- Deploy workflow run succeeded: both `validate` and `deploy` jobs green. IndexNow ping returned HTTP 202 (accepted).
- **Live URL**: https://service-harvest.github.io/petes-plumbing/ — confirmed reachable (200) and serving the real site.
- **Domain note**: the `CNAME` file references `hexorasystems.com` per intake ("for testing purposes"). That domain is a real, unrelated business's live site (Hexora Systems, IT consulting) — DNS was never pointed at this deployment, and GitHub's Pages API confirms no custom domain is actually verified/active (`cname: null`). The site is only genuinely live at the GitHub Pages URL above. A real client launch needs an actual domain the client owns, with DNS pointed at GitHub Pages, before the custom-domain step applies.
- Lighthouse spot-check (homepage + Drain Cleaning, a Tier-1 page): SEO 100/100 on both, Accessibility 95, Performance 87-88, Best Practices 73 (likely dragged down by the third-party GHL scripts, outside this build's control). Cumulative Layout Shift 0.24-0.27 — above Google's "good" threshold (0.1), worth a closer look in a future pass (candidate causes: image dimensions vs. rendered size, or the chat widget injecting late). on_page_instant_pages on the homepage: onpage_score 97.44, single clean H1, well-structured H2/H3 hierarchy, canonical/OG/Twitter tags all correct, 200 status, gzip-compressed, cacheable.

## Image Notes
(Generated vs. substituted images, from Phase 7)
- Gemini confirmed working as of 2026-07-16 (see Connection Status above). Generation batches completed so far, all succeeding on first attempt with zero SVG substitutions:
  - Homepage, Services Hub, About, Contact — 7 images
  - Gasfitter, Gas Line Repair, Gas Water Heater Installation, Septic Services, Septic Tank Service — 12 images
  - Bathroom & Kitchen Remodeling (+2 children), Water Heater Installation, Emergency Plumbing Repair — 11 images
  - Plumber service batch B (Hot Water System Repair through Sink Installation, 12 pages) — 24 images
  - Drainage Service (+7 children) — 18 images
  - Plumber service batch C (Boiler Installation through Backflow Testing, 11 pages) — 22 images (required one resume after an initial stalled status; verified all 11 pages have real images on disk before logging)
  - Plumber service batch A (Tankless Water Heater Installation through Shower Valve Replacement, 12 pages) — 24 images
  - **Final tally: 119 images across all 57 pages, zero permanent failures, zero SVG substitutions needed.** Verified sitewide: every page has exactly one hero/OG image, every `<img>` tag has alt/width/height, every `src` resolves to a real file on disk, no leftover temp files in `site/assets/img/`.

## Post-launch fix #3 (two bugs, one deploy): distorted images sitewide + popup auto-triggering again

**Bug A — every one of the 119 images was distorted (stretched/squished).**
Root cause: Gemini's `gemini-2.5-flash-image` model returns its own native output size (a 1024×1024 square) regardless of the prompt or the width/height requested — it does not honor an arbitrary target aspect ratio. The original `scripts/generate-image.js` resized with `cwebp -resize <w> <h>`, which forces the source directly to the exact target width AND height — a non-uniform stretch. Confirmed empirically: generated a test image of a plain red circle, ran it through the original resize command, and it came out as a squashed ellipse — proof the distortion was in the resize step, not a one-off content issue. Every image on the site had this same defect, since every image went through the same resize call regardless of its content or page.

Every declared `width`/`height` attribute in the HTML *did* exactly match its file's real pixel dimensions (verified sitewide, zero mismatches) — that check alone wasn't enough to catch this, since the bug was inside the file's pixel content, not in a mismatch between the file and its HTML attributes.

Fix: rewrote the resize step to scale-to-cover (uniform scale so the image fully covers the target box, preserving aspect ratio) then center-crop to the exact target size — the same effect as CSS `object-fit: cover`, baked into the file. Re-tested the same red-circle case after the fix: it stayed a circle. Regenerated all 119 images in place (same filenames, using each image's existing alt text as the regeneration prompt, since alt text already accurately described what was generated) — zero failures. No HTML changes were needed, only the image files themselves.

Updated `pipeline/phase-07-images.md` with an explicit warning about this exact pitfall (Gemini's native size vs. requested dimensions, stretch vs. cover-then-crop) so future client builds don't reintroduce it. Note: the corrected `scripts/generate-image.js` lives in this client repo only — it was never copied into `pipeline-template/scripts/`, so a fresh client build would need to re-derive this fix (or have it copied over) unless that's done separately.

**Bug B — the free-estimate popup was back to auto-showing on every page load**, the opposite of the previous fix's intent. Root cause: the embed's `data-trigger-type="click"` / `data-trigger-value=".request-estimate-cta"` configuration (set in the prior fix) is **not actually respected by GHL's `form_embed.js` script** — confirmed live: inspected the deployed page's DOM and found those exact attribute values correctly present and unchanged on the iframe element, yet the widget still auto-created and displayed its wrapper/overlay on page load regardless. In other words, the config was set correctly; the third-party script simply doesn't honor it. This means the previous fix's assumption (that `data-trigger-type="click"` is a supported, working value) was wrong, and there's no way to verify that class of undocumented third-party config against real documentation — the only way to know is to test the actual behavior, which is what caught this.

Fix: stopped relying on the embed's own trigger system entirely. Added a small inline script (present sitewide, alongside the embed) that actively suppresses the widget's own wrapper/overlay elements (`#popup-xHJhkUk8Wps0ag0Dokml-wrapper` and `-overlay`, both identified by inspecting the live DOM while the popup was open) via a `MutationObserver` plus timed sweeps, immediately hiding either one the instant the third-party script creates or shows them — and only removes that suppression when a genuine click on `.request-estimate-cta` is detected, at which point the popup is allowed to show and stays allowed for the rest of that page view. This does not depend on the third-party script's own trigger semantics at all; it directly controls the DOM elements the script creates.

**Follow-up found during live verification**: after deploying the suppression script, confirmed on the live site that auto-show was fixed, but clicking "Request a Free Estimate" also did nothing visible — the widget's own `.ep-overlay` CSS class sets `display: none` as its *default* (confirmed by inspecting the live page's injected stylesheet), so removing our suppression override just fell back to that same hidden default instead of actually showing it. Fixed by having the click handler explicitly set `display: flex` on the overlay and `display: block` on the wrapper (matching what the widget itself sets when it auto-shows) rather than merely removing our override. Verified this exact fix directly in the live page's console before updating the file, then confirmed the corrected script produces the same result end-to-end after redeploying.

**Final end-to-end live verification** (commit `e6803a3`, deployed and confirmed green): loaded the homepage fresh (no cache) — no popup auto-opened, hero image rendered fully proportional (no stretch/squish). Clicked "Request a Free Estimate" — the full lead-capture form (Full Name, Email, Phone, Address, City, State fields) rendered correctly inside the popup. Repeated on the Services hub page (a non-homepage page, reached via its real relative path) — same result: no auto-popup on load, hero image proportional, confirming both fixes hold sitewide and not just on the page originally used to diagnose them. Re-ran `scripts/validate.js`: clean, 0 failures. Both bugs are closed.

## Post-launch fix #4: design-system modernization (spacing, container width, typography, color/depth/icons)
Client feedback: the live site looked plain/dated, with noticeably wide desktop side margins. Diagnosed `main.css` directly (single flat 1140px container reused everywhere, one flat alt-background stripe, single-weight system-font headings, zero box-shadows, no hover motion) before changing anything, and reported the diagnostic back for sign-off. Root cause was judged to be mostly `phase-04a-design-system.md`'s Task 1 quality bar itself ("consistent and WCAG AA accessible" is a floor, not a modern-feel target) rather than this client's thin intake — the intake did ask for "modern, polished, not generic," but the phase file never translated that into a concrete bar for spacing generosity, depth, type pairing, or full-bleed section rhythm, so even a client with strong brand direction would have gotten a similarly plain result.

Implemented all four proposed areas in `site/assets/css/main.css`:
- **Spacing**: added `--space-6` (a `clamp(3.5rem, 6vw, 5.5rem)` tier) for section vertical padding, replacing the flatter `--space-4` (2.5rem); `clamp()`-based container/header/footer side padding so it scales on ultra-wide screens.
- **Container/full-bleed**: `--container-max` raised from 1140px to 1280px; body copy (`p`) capped at a separate 68ch measure independent of container width for readability; section background colors already applied to the full-width `<section>` element (not just the inner `.container`), so alternating section colors now read as full-bleed rhythm rather than a single flat stripe.
- **Typography**: headings now 700–800 weight with tightened letter-spacing (-0.01 to -0.02em, tighter at larger sizes) instead of being distinguished from body text by color alone; h1/h2 sizes bumped slightly. No new typeface or webfont — the system-font stack (Layer 1 requirement) is unchanged.
- **Color/depth/icons**: added a 3-tier shadow scale (`--shadow-sm/md/lg`) applied to buttons, cards, and hero/content images; subtle gradients on the hero background and CTA banner (same two brand colors, no new colors introduced); soft hover lift + shadow increase on buttons/cards via pure-CSS `transition` (wrapped in a `prefers-reduced-motion: reduce` override); decorative checkmark icons on trust badges via a CSS `mask` inline-SVG data-URI (`.trust-badges li::before`) — purely decorative, not in the accessibility tree, no alt text, no new image files or network requests.

**Accessibility re-check**: none of the color values changed (only weight/letter-spacing/shadow/spacing), and WCAG contrast is a function of color, not weight — recalculated the two contrast-bearing pairs by hand (white text on `--color-primary` ≈9.2:1, white text on `--color-accent` ≈5.7:1) and confirmed both still clear AA's 4.5:1 minimum for normal text, unchanged from before this pass.

**Layer 1 check**: no new render-blocking resources (still one `main.css`, no webfont added), no new HTTP requests (icons are inline `data:` URIs, not files), image LCP/lazy-load logic completely untouched (no HTML changes), all content remains real static HTML in the initial response (CSS-only change, zero HTML edits).

**Template fix**: updated `pipeline/phase-04a-design-system.md` Task 1 with the same five-part quality bar (generous `clamp()`-based spacing, ~1200–1320px container with a narrower body-copy measure, full-bleed alternating section backgrounds, weight/letter-spacing-based type pairing, subtle depth + reduced-motion-safe hover states, decorative-only inline-SVG icons) so this is the default baseline for every future client, not a one-off fix — including an explicit instruction to re-verify AA contrast after applying it rather than assume it.

**Verified in-browser**: loaded the homepage locally — bold tightly-tracked h1, gradient hero background, shadowed rounded buttons/hero image, and orange checkmark icons on trust badges all render as intended, no layout breakage. Re-ran `scripts/validate.js`: clean pass, 0 failures (only pre-existing non-blocking title/meta-length warnings, unrelated to this change). Pushed and confirmed the deploy workflow succeeded.

## Post-launch fix #5: Services hub only linked 15 of 53 pages in body copy
Client diagnosis request found that `/services` linked only 15 of the 53 real category/service pages from its body copy — the other 38 (mostly the 37-page uncategorized "Plumber" bucket, which has no dedicated category page) were absent, even though `scripts/validate.js` passed cleanly. Root cause: `phase-06-content-drafting.md`'s Services hub spec said "2–4 of its most relevant service pages" per category section, which is reasonable for the 4 real categories (12 children total) but was applied literally to the 37-page uncategorized bucket too, surfacing only 3 of 37. `scripts/validate.js`'s orphan-page check couldn't catch this because it only requires *some* incoming body link from anywhere sitewide (satisfied by routine sibling cross-links), not specifically a link from the hub.

**Fix applied**:
- `pipeline/phase-06-content-drafting.md`: added a "full coverage, no exceptions" requirement (every category/service page, including catch-all-bucket pages, needs at least one in-body hub link) and a "large flat groupings need sub-structure" requirement (buckets over ~8–10 pages get their own H2 with readable H3 sub-groupings by genuine service type, not a sparse sample or a link wall).
- `scripts/validate.js`: added a new hard-fail check — every non-exempt page must have an in-body link specifically from `/services`, not just be reachable sitewide. Tested against the pre-fix hub first and confirmed it correctly flagged all 38 missing pages before any HTML changed.
- `pipeline/phase-12-validation-gate.md`: documented the new check in the Content/SEO checklist.
- `site/services/index.html`: restructured the "Plumbing" section (the 37-page catch-all) into 4 readable H3 sub-groups — Emergency/Leak/Pipe Repair (11 pages), Water Heating/Softening/Filtration (6), Fixture/Appliance/Bathroom-Kitchen Installs (12), Specialty/Maintenance/Commercial (8) — each with real prose and unique in-body anchor text per page. Also expanded the Drainage Service section to link all 7 children (was 3 of 7) since the category itself needed full coverage too. All 53 pages now get a direct in-body hub link.
- `ledgers/anchor-ledger.md`: added the 38 new hub→page rows so the ledger stays the authoritative record of every anchor used.

**Verified**: re-ran `scripts/validate.js` after the fix — clean pass, including the new hub-coverage check (0 missing pages) and no duplicate-anchor-text collisions introduced by the new links.

## Post-launch fix #6: five audit findings fixed + validate.js hardened against recurrence
Following up on the systematic spec-vs-actual audit, fixed all five confirmed findings and added a permanent validate.js check for each so they can't silently regress on this or future client builds:

1. **LCP preload missing** (Phase 7 requirement never implemented): added `<link rel="preload" as="image" href="[hero image]">` to all 57 pages, derived per-page from each page's actual non-lazy-loaded hero/OG image. Lighthouse re-check below.
2. **Apostrophe-truncation bug in `validate.js`'s `extractTag()`**: the meta-description and canonical regexes used a bare `["'](.*?)["']` class, so a straight apostrophe inside a double-quoted value (e.g. "what's") was misread as the closing quote, silently truncating the match (confirmed on 21+ pages, one as short as 8 "characters" reported for a real 130+ char description). Fixed by capturing the actual quote character and backreferencing it (`(["'])(.*?)\1`), and made `extractTag()` accept a `groupIndex` so any future caller extracting a quoted attribute uses the same robust pattern.
3. **Sitemap/canonical trailing-slash mismatch**, and `validate.js`'s doc comment claiming a cross-check that didn't actually exist: standardized on no-trailing-slash (matching the existing canonical convention) across all of `sitemap.xml`, then implemented a real byte-identical cross-check between each sitemap `<loc>` and its page's canonical tag (hard fail on mismatch, not just normalized-equal). Verified the new check actually catches a reintroduced mismatch before considering it done.
4. **Incomplete favicon set** (Phase 4a Task 2 never fully implemented): rasterized the existing placeholder SVG mark into the required 16×16, 32×32, and 180×180 apple-touch-icon PNGs (plus 192×192/512×512 for the manifest) via `qlmanage`, added `site/assets/site.webmanifest`, and added the full 5-tag favicon `<link>` block to all 58 files (57 pages + 404). Added a new validate.js check requiring all 5 tags present on every page and every referenced file to actually exist on disk — verified it catches a removed tag.
5. **H2-count and FAQ-count violations** on `emergency-plumbing-repair` and `water-heater-installation` (6 main H2 sections / 8 FAQs vs. the 5-section / 5–6-FAQ norm every other page follows): consolidated each page down to 5 main H2 sections (merging overlapping content rather than deleting it — e.g. water-heater-installation's standalone "signs you need a new water heater" section folded into its "when to call a pro" section, which already covered the same symptoms) and trimmed each to 6 FAQs, updating both the visible FAQ markup and the FAQPage JSON-LD to match. Added a new validate.js check requiring exactly 5 main H2s (excluding the TOC heading and the FAQ section's own heading) and 5–6 FAQs on every category/service page — verified it catches a reintroduced 6th section.

Also checked the "stylistic" reduced-motion CSS item flagged by the audit (hover `transform` not disabled alongside `transition` under `prefers-reduced-motion`) — found it was already correctly handled in `main.css` from the earlier design-system pass (both `transition: none` and `transform: none` are set), so the audit finding was stale; no change needed.

**Lighthouse CLS re-check** (same methodology as Phase 13's spot-check — homepage + Drain Cleaning, a Tier-1 page):
- **Before this fix** (currently-live site, i.e. after the earlier image-distortion/design-system fixes but before LCP preload): homepage CLS 0.001, Drain Cleaning CLS 0.001 — both already effectively zero, a big improvement over Phase 13's original 0.24–0.27. That earlier gain traces to the `aspect-ratio` CSS added on hero images during the design-system modernization pass, which reserves layout space before the image loads regardless of preload.
- **After this fix** (LCP preload deployed): homepage CLS 0.001, Drain Cleaning CLS 0.001 — unchanged, since there was no meaningful CLS left to improve. The preload's real benefit here is faster LCP paint time, not CLS (CLS was already solved by a different mechanism). Both pages score Performance 100, SEO 100, Accessibility 100 in this pass.

Word-count guidance (Part B, template-only): added a 1500–2500-word target for category/service pages to `pipeline/phase-06-content-drafting.md`, explicitly conditioned on genuine content/research depth rather than padding — this is a template change for future clients only; no content on the live petes-plumbing build was expanded or re-researched as part of this.

**Verified**: re-ran `scripts/validate.js` — clean pass, including all 3 new hard-fail checks (sitemap/canonical exact-match, favicon coverage, H2/FAQ structural consistency). Pushed and confirmed the deploy workflow succeeded.

## Post-launch fix #7: thin homepage cards + Services hub card-grid exception

**1. Homepage premium cards were too thin.** The 6 homepage-linked category/service cards each had a single generic sentence and no image. Rewrote each card with a real 3–4 sentence paragraph pulling a specific detail already established for that destination page (e.g. the Drainage Service card now references older Westchester homes' narrower original pipes and root intrusion from mature trees — details already logged for that page in `content-ledger.md`, not new local claims), and added each card's destination page's own existing hero/LCP image (reused, not regenerated), lazy-loaded since it's not the homepage's own LCP image. Added `.card-image` CSS (image bleeds to the card's top edge, rounded to match the card). Updated `phase-06-content-drafting.md`'s homepage section with an explicit sub-requirement: real paragraph pulled/summarized from the destination page's own content (checked against `content-ledger.md`, never a verbatim large-passage copy), plus the destination's existing hero image reused — so this is the default bar for every future client's premium homepage cards, not a one-off fix.

**2. Services hub — added a hub-only exception to the in-body-prose-link rule.** The hub was following the same "contextual prose links" pattern as every other page, which doesn't fit a 53-page directory page. Rewrote `phase-06-content-drafting.md`'s Services hub section: this one page (and explicitly *only* this page — every other page, including the 4 category pages themselves, keeps the existing full in-body-link requirement) now uses a brief category framing sentence followed by a card grid (title = the page's own service name as real anchor text, description = the page's own meta description, "Learn More" link styled/exempted the same way the repeated CTA buttons already are). The spec is explicit that this is not a return to a sparse link wall — full coverage of every page and the large-bucket sub-grouping requirement from the previous hub-coverage fix both still apply, just rendered as card grids instead of prose paragraphs.

Applied to the live hub: all 53 cards present (4 category framing links + 49 service cards across the 4 categories' own grids and the 4 sub-grouped "Plumbing" grids, unchanged from the prior fix's grouping). Reconciled `anchor-ledger.md`: replaced all 54 old `/services`-sourced rows (prose-phrase anchor text) with new rows matching the actual card title text and category framing-sentence anchors — re-checked for sitewide duplicates via `scripts/validate.js`, none found.

**Verified**: re-ran `scripts/validate.js` — clean pass. Specifically confirmed the hub-coverage check (which only excludes `<nav>`/`<footer>`, not the main-content card grid) still passes with the new structure, and that it wasn't just assumed to work.

## Post-launch fix #8: homepage priority picks redone as full-width sections, not cards
Fix #7's homepage-card rewrite wasn't the actual ask — client wanted each of the 6 priority picks to be its own full-page-width landing-page-style section stacked down the homepage, not a card in a grid. Replaced the entire `card-grid` block in `site/index.html` with 6 stacked `<section>` blocks, each: an H2 naming the category/service, 2–3 real paragraphs (condensing or expanding on that page's own already-established local angle — e.g. the Drainage Service section expands on the sewer-camera-inspection/root-intrusion detail from that page's own "Sewer line diagnosis and repair" section, reworded rather than copied; checked against `content-ledger.md`'s logged angles for each page, no new/colliding local claims introduced), that page's own existing hero image (reused, not regenerated, laid out side-by-side with the text and lazy-loaded since it's not the homepage's own LCP image), and a "Learn More" CTA button to the dedicated page. Image/text sides alternate section to section (`.feature-split` / `.feature-reverse` CSS) so six stacked sections don't repeat the identical layout, and section backgrounds alternate to preserve the page's established rhythm. Removed the now-dead `.card-image` CSS (was only ever used by the card layout this replaces) and added `.feature-split` in its place. Kept each section's pre-existing real in-body anchor link (the same phrases already logged in `anchor-ledger.md` for `/` → each destination) inside the new paragraph prose, so the "links live in body content" rule still holds independent of the CTA button.

Updated `pipeline/phase-06-content-drafting.md`'s homepage section to require this exact structure (H2, 2–3 paragraphs, reused hero image in an alternating side-by-side layout, CTA button + genuine in-body link) for every future client's premium homepage sections — replacing the card-based guidance from fix #7, which undersold what "premium real estate" should look like.

**Verified**: re-ran `scripts/validate.js` — clean pass, no new duplicate-anchor or broken-link issues from the restructure.

## Post-launch fix #9: template-sync + newly-found QA fixes

**Part A — synced every session fix back to `pipeline-template/`.** A full audit found that every phase-file/script fix made this entire session lived only in this client's own copy — `pipeline-template/` (the actual master the `new-client-website` skill duplicates for every new client) had never been touched, since it isn't even a git repo and nothing in the workflow ever wrote back to it. Diffed all 7 files this session had touched (`phase-04-repo-scaffold.md`, `phase-04a-design-system.md`, `phase-06-content-drafting.md`, `phase-07-images.md`, `phase-10-ghl-embeds.md`, `phase-12-validation-gate.md`, `scripts/validate.js`) between the two copies, confirmed every difference was a legitimate reusable fix (no client-specific content mixed in), and copied each wholesale into `pipeline-template/`. Also added `scripts/generate-image.js` (the scale-to-cover image script, previously described only in prose in `phase-07-images.md` but never shipped as an actual file) to `pipeline-template/scripts/`, and fixed a stale "cards" reference in `phase-06-content-drafting.md`'s homepage structure summary line (leftover wording from before the full-width-section redesign). Verified the sync by re-running the same grep-for-session-language check that originally found zero matches in `pipeline-template/` — it now finds matches in all the expected files.

**Part B — fixed newly-found bugs/gaps, applied to both `pipeline-template/` and this client's live site:**
1. **GHL popup auto-show gotcha, still undocumented after the sync.** `phase-10-ghl-embeds.md` only ever documented the surface config (`data-trigger-type="click"`), never the actual finding that GHL's script ignores it, nor the `MutationObserver` suppression script, nor the overlay `display:none` CSS-default gotcha discovered earlier this session. Added a full "Known gotcha" section with a copy-pasteable reference implementation of the suppression/reveal script, plus an explicit instruction to verify both behaviors live before considering the phase done — in both copies.
2. **`generate-image.js` now a real file**, not just prose (see Part A).
3. **"The the..." doubled-word bug**, root-caused: not a wrong phase-file instruction, but a genuine QA-enforcement gap — Phase 8's check 4 ("natural, human-sounding flow") should have caught this but didn't in practice. Fixed the 4 originally-found live instances (`grease-trap-cleaning`, `hot-water-system-repair`, `boiler-installation`, `urinal-installation`) by moving the duplicated word inside the anchor boundary. Added a new generalizable `scripts/validate.js` check (any word immediately before an anchor tag that case-insensitively matches the anchor text's first word is now a hard fail) — running it immediately surfaced **3 more real instances** the original manual QA pass had missed (`frozen-pipe-repair`, `slab-leak-repair`, `water-heater-installation`), all fixed the same way. Confirmed zero remaining after the fix.
4. **Breadcrumbs**: 16 pages had zero visible breadcrumb markup despite BreadcrumbList schema being present; another 15 (plus About/Contact/Services) used a non-semantic `<p class="breadcrumbs">` instead of `<nav aria-label="Breadcrumb">`. Normalized all 56 pages sitewide to the semantic `<nav>` form, inserting it fresh (derived from each page's own H1) where missing entirely. Verified zero `<p class="breadcrumbs">` and zero missing breadcrumbs remain.
5. **Backflow-testing's certification claim** ("...which is the kind of certification we maintain...") wasn't traceable to anything in `intake/intake-completed.md` — the intake only supports generic "licensed and insured" / "Westchester County licensed plumber," not a specific PHCC-linked credential. Reworded to the honest, sourced claim (staying current on industry training as part of being a licensed plumber) without implying a specific unverified certification.
6. **Recycled "finished/unfinished basement" local angle.** Re-checked all 4 pages the audit flagged — `rainwater-tank-installation` (real angle: larger lots/gardens) and `water-filtration-system-installation` (real angle: taste/contaminants vs. hardness) turned out to already have genuinely distinct angles; only `sump-pump-installation` and `laundry-room-plumbing` actually shared the same core premise ("finished basements are common in Westchester"), confirmed by their near-identical opening sentences and by `content-ledger.md`'s own two entries reading as distinct summaries despite the underlying pages saying the same thing first. Rewrote `laundry-room-plumbing`'s local-factors section around a genuinely different angle (older Armonk homes never originally plumbed for a convenient laundry hookup at all, vs. sump-pump's basement-flooding angle), updated its TOC label and `content-ledger.md` entry accordingly. Tightened Phase 8 check 5's wording to require comparing the actual underlying premise two pages build on, not just whether their ledger summary phrases read differently on paper — the exact gap that let this through originally.
7. **README.md**: clarified that `/snippets/` content (not the underlying path convention) is per-client and never ships pre-populated in the template, and replaced the stale pre-launch "Open items" checklist with an "Environment setup checklist" reflecting that a full build-and-fix cycle has actually happened now.

**Verified**: re-ran `scripts/validate.js` on the client copy after all fixes — clean pass, including the new duplicated-word-boundary check. Confirmed `pipeline-template/` and this client's copy are now byte-identical across every phase file and script either has ever touched. Pushed and confirmed the deploy workflow succeeded.

## Post-launch: custom domain connected (hexorasystems.com, apex canonical)
Connected the client's real domain end to end:
- Client added DNS at their registrar: 4 apex `A` records to GitHub Pages' IPs, 1 `www` `CNAME` to `service-harvest.github.io`. Verified propagation directly against both the local resolver and `8.8.8.8` — all 5 records resolved correctly.
- Diagnosed the client's first attempt (generic GitHub 404 + "Not Secure") correctly: DNS was fine, but the GitHub Pages custom-domain activation step (`gh api .../pages -X PUT -f cname=...`) hadn't been run yet — confirmed via the Pages API showing `cname: null` before, `cname: "hexorasystems.com"` after. DNS pointing at GitHub's IPs is necessary but not sufficient; GitHub also needs to be told which repo owns that hostname.
- Ran the activation call with apex (`hexorasystems.com`) as canonical, per the client's explicit choice. HTTPS certificate auto-approved for both `hexorasystems.com` and `www.hexorasystems.com` within ~15 seconds. Enabled `https_enforced` (required a JSON boolean via `-F`, not `-f`, which sends a string).
- Verified live, not assumed: apex serves the real site (200) over HTTPS; `www` returns a real `301` to the apex (not just "both happen to load"); plain HTTP redirects (301) to HTTPS. Confirmed canonical tags, OG tags, and `sitemap.xml` all consistently reference `https://hexorasystems.com/...` on both the homepage and a sampled deep page.
- **Template fix**: `phase-13-deploy.md` step 8 was previously just "note the DNS records needed" with no canonicalization guidance — this only became a real, complete process because the client asked for it manually. Rewrote it as a required sub-flow (gated on intake's domain + registrar-access fields) covering: asking which form is canonical, giving both DNS record sets (not just the canonical one), the activation API call, live redirect verification (not assumed), and the canonical/sitemap/OG re-check — synced to both this client's copy and `pipeline-template/` so every future client with a real domain gets this automatically.
